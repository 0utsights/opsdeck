# OpsDeck's carousel rotates independent tmux windows. Keep machine-specific paths
# and project names in your private copy at ~/.config/opsdeck/carousel.conf.
interval_seconds=20
session_name=opsdeck

window=opsdeck|opsdeck
window=system-monitor|btop
window=project-tui|replace-with-your-project-command
